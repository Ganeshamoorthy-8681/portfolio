name: Deploy Angular App (from subfolder) to GitHub Pages

on:
  push:
    branches:
      - main  # 👈 change if your default branch is different

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1️⃣ — Checkout your repo
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      # Step 2️⃣ — Setup Node.js
      - name: Setup Node.js 🧩
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Step 3️⃣ — Move into Angular project folder
      - name: Change working directory 📂
        run: cd enhanced-portfolio/portfolio-app

      # Step 4️⃣ — Install dependencies
      - name: Install dependencies 📦
        run: |
          cd enhanced-portfolio/portfolio-app
          npm install

      # Step 5️⃣ — Build Angular app
      - name: Build Angular app 🏗️
        run: |
          cd enhanced-portfolio/portfolio-app
          npm run build -- --configuration production --base-href "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"

      # Step 6️⃣ — Deploy to GitHub Pages
      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: enhanced-portfolio/portfolio-app/dist/portfolio-app/browser  # 👈 replace with actual dist folder name
